import 'package:flutter/cupertino.dart';

class AddingCarView extends StatelessWidget {
  const AddingCarView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Add Car');

  }
}